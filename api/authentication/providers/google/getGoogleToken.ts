/**
 * Retrieves a new token from Google, if the user did not authorized the app it shows an authorization screen
 * executed by the Google API.
 */
export async function getGoogleToken(): Promise<string | null> {
   // TODO: Implement
   return "";
}
